#include<stdio.h>
int main(){
	int marks[10];

	for(int i=0; i<10; i++){
		printf("Enter the mark of the student at %d no of array\n",i);
		scanf("%d",marks[i]);
	}
	for(i=0; i<10, i++);
		printf("Marks of student at %d no in array is %d\n", i, marks[i]);
	
	return 0;
}